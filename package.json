{
  "name": "static-site",
  "version": "1.0.0",
  "description": "A command line interface to assist in creating Readme markdown files.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michael Olshewski",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^6.3.1"
  }
}
